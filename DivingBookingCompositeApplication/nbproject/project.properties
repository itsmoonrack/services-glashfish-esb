build.classes.dir=${build.dir}/jar
build.dir=build
build.generated.dir=${build.dir}/generated
com.sun.jbi.routing=true
com.sun.jbi.routing.bc.autoconnect=true
com.sun.jbi.sa.internal.routing=true
dist.dir=dist
dist.jar=${dist.dir}/DivingBookingCompositeApplication.zip
jbi.content.additional=${reference.LunchService.dist_se}
jbi.content.component=sun-bpel-engine
jbi.service-assembly.description=Represents the Service Assembly of DivingBookingCompositeApplication
jbi.service-assembly.id=DivingBookingCompositeApplication
jbi.service-unit.description=Represents this Service Unit
meta.inf=${source.root}/conf
project.LunchService=../LunchService
reference.LunchService.dist_se=${project.LunchService}/build/SEDeployment.jar
resource.LunchService=${project.LunchService}/setup
resource.ProcessManager=${project.ProcessManager}/setup
source.encoding=UTF-8
source.root=src
src.build.dir=${source.root}/jbiServiceUnits
src.dir=${source.root}/jbiasa
test.dir=test
test.results.dir=${basedir}/test/results
